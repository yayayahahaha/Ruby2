<ul>
	<%= nav_li 'Home', root_path %>
	<%= nav_li 'About', about_path %>
	<%= nav_li 'Math', math_path %>
	<%= nav_li 'Form', form_path %>
</ul>

<!-- the partial's name has to be start at '_' (under line)
	if the render page didn't specify the parent dir, it will find the partail in the controller which the view belongs to -->
